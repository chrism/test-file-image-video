<h2 id="title">Rendering images and videos testing</h2>

{{file-chooser name="file-chooser" id="file-chooser" action="addFile"}}

{{#each allAssets as |item|}}
  {{component item.componentType file=item.file}}
{{/each}}
